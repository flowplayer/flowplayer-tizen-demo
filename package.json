{
  "name": "flowplayer-tizen-demo",
  "version": "1.0.0",
  "description": "Flowplayer Demo for Tizen development",
  "author": "Flowplayer Authors <support@flowplayer.com>",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "@types/node": "^18.15.5",
    "parcel": "^2.8.3",
    "process": "^0.11.10"
  },
  "dependencies": {
    "@extractus/feed-extractor": "^6.2.1",
    "@flowplayer/player": "^3.5.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "watch": "yarn clean && parcel watch index.html --public-url ./",
    "build": "yarn clean && parcel build index.html --public-url ./"
  }
}
